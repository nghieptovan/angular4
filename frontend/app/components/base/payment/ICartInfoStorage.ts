export interface ICartInfoStorage{
    cartId: any,
    paymentMethod: any,
    shipping_duration: any,
    orderSuccess: any,
    differentBillingAddress: any,
    billingAddress: any,
    shippingAddress: any,
    shipping_comments: any,
    vatInfo: any,
    shippingRules: any,
    cart: any,
    selectedPaymentType: any,
    shipping_method_code: any

    // storageCartId(cartId);
    // storagePaymentMethod(paymentMethod);
    // storageShipping_duration(shipping_duration);
    // storageOrderSuccess(orderSuccess);
    // storageDifferentBillingAddress(differentBillingAddress);
    // storageBillingAddress(billingAddress);
    // storageShippingAddress(shippingAddress);
    // storageShippingComments(shipping_comments);
    // storageVatInfo(vatInfo);
    // storageShippingRules(shippingRules);
    // storageCart(cart);
    // storageSelectedPaymentType(selectedPaymentType);

}